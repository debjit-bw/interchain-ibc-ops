{
  "name": "ibc-operation-exam",
  "version": "1.0.0",
  "description": "",
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prebuild": "rm -rf ./dist/",
    "build": "tsc",
    "prebuild:prod": "rm dist.zip && npm run build",
    "build:prod": "zip -r dist.zip dist/* node_modules"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@cosmjs/stargate": "0.28.13",
    "knex": "2.3.0",
    "mysql2": "2.3.3",
    "typescript": "4.8.4"
  },
  "devDependencies": {
    "@types/node": "18.8.2",
    "dotenv": "16.0.3",
    "ts-node": "10.9.1"
  }
}